name: Hello World!

on:
  push:
    branches:
      - main

env:
  DAY_OF_WEEK: Monday

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World! Today is $DAY_OF_WEEK"

      - name: list all env variables
        run: env
